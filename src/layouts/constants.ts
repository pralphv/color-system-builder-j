export enum Pages {
  Home = "/",
  Login = "/login",
  Logout = "/logout",
  Register = "/register",
  ForgotPassword = "/forgot-password",
  Verify = "/verify",
  MySchemes = "/my-schemes",
  Schemes = "/schemes/:id"
}
